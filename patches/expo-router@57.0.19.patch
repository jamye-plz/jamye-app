diff --git a/build/fork/NavigationContainer.js b/build/fork/NavigationContainer.js
index 3db5ea4f3106bff3b3bd5799e41914be47ae5052..bcd7df744abdc568b96d123468f467ecdc9be5a6 100644
--- a/build/fork/NavigationContainer.js
+++ b/build/fork/NavigationContainer.js
@@ -41,11 +41,32 @@ function NavigationContainerInner({ direction = react_native_1.I18nManager.getCo
     (0, useDocumentTitle_1.useDocumentTitle)(refContainer, documentTitle);
     (0, imperative_api_1.useImperativeApiEmitter)(refContainer);
     const [lastUnhandledLink, setLastUnhandledLink] = react_1.default.useState();
+    const hasMountedRef = react_1.default.useRef(false);
+    const pendingUnhandledLinkRef = react_1.default.useRef(null);
+    const onUnhandledLinking = react_1.default.useCallback((path) => {
+        if (hasMountedRef.current) {
+            setLastUnhandledLink(path);
+            return;
+        }
+        pendingUnhandledLinkRef.current = { path };
+    }, []);
+    react_1.default.useEffect(() => {
+        hasMountedRef.current = true;
+        const pendingUnhandledLink = pendingUnhandledLinkRef.current;
+        pendingUnhandledLinkRef.current = null;
+        if (pendingUnhandledLink !== null) {
+            setLastUnhandledLink(pendingUnhandledLink.path);
+        }
+        return () => {
+            hasMountedRef.current = false;
+            pendingUnhandledLinkRef.current = null;
+        };
+    }, []);
     const { getInitialState } = (0, useLinking_1.useLinking)(refContainer, {
         enabled: isLinkingEnabled,
         prefixes: [],
         ...linking,
-    }, setLastUnhandledLink);
+    }, onUnhandledLinking);
     const linkingContext = react_1.default.useMemo(() => ({ options: linking }), [linking]);
     const unhandledLinkingContext = react_1.default.useMemo(() => ({ lastUnhandledLink, setLastUnhandledLink }), [lastUnhandledLink, setLastUnhandledLink]);
     const onReadyForLinkingHandling = (0, useLatestCallback_1.default)(() => {
